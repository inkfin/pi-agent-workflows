/**
 * plan.ts — DEPRECATED
 *
 * `/plan` is owned by `.pi/extensions/orchestrator`.
 * This module is kept only as a reference for the old prompt-prefill helper.
 * It is no longer registered from commands/index.ts.
 */

export {};
